#include "main.h"
/**
 *main - Entry point
 *Programm that prints alphabet
 *Return: 0 (success)
 */
int main(void)
{
print_alphabet_x10();
int times;
int i;
 for (i = 0; i < times; i++)
{
char abc;  
for (abc = 'a'; abc <= 'z'; abc++)
{_putchar (abc);
}
_putchar ('\n');
}
return (0);
}
