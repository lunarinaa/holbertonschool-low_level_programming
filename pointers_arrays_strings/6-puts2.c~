#include "main.h"
/**
 * puts2 - function to print every other character of a string.
 * @str: string
 */
void puts2(char *str)
{
int i = 0;
while (s[i] != '\0')
{
_putchar (s[i]);
i += 2;
}
_putchar ('\n');
}
