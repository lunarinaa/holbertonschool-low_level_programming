#include "main.h"
/**
 * rev_string - function to reverse a string
 * @s: parameter
 */
void rev_string(char *s)
{
int len, i;
len = 0;
while (s[len] != '\0')
len++;
for (i = len - 1; i >= 0; i--)
{
	_putchar(s[i]);
break;
}
}
